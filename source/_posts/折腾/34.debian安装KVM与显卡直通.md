## 背景提要

+ 本来是计划WSL实现Windows与Linux双持的，但是奈何WSL的BUG是在太多，且内存占用大，空转都能占用3G内存，放弃该方案
    + [32.WSL安装.md](32.WSL%E5%AE%89%E8%A3%85.md)
+ 安装双系统，Windows的抽风更新我担心会破坏Linux，不推荐
+ 最后只剩下Linux作为宿主机，在Linux里安装Windows虚拟机，且需要实现显卡直通
+ 首先安装Debian，[30.安装Debian手记.md](30.%E5%AE%89%E8%A3%85Debian%E6%89%8B%E8%AE%B0.md)其中关于N卡驱动先不操作

## 方案

+ 主系统是Debian13，不安装桌面，大部分时候使用的是Linux，通过SSH控制
+ Linux安装Docker，Docker再使用显卡，避免弄脏系统
+ 在Linux基础上，通过KVM虚拟机+PCI显卡直通，部署Windows11物联网版虚拟机
+ KVM负责运行虚拟机，libvirt负责调度虚拟机，cockpit负责界面化管理
+ GPU归属不写死绑定，由libvirt hook脚本动态切换：开Windows时卸nvidia交给vfio，关Windows时收回给nvidia

## BIOS检查

### 虚拟化

| 项目	                                    | 设置        | 	说明                        |
|----------------------------------------|-----------|----------------------------|
| Intel Virtualization Technology (VT-x) | 	Enabled	 | CPU虚拟化                     |
| VT-d / Intel VT for Directed I/O	      | Enabled   | 	显卡直通                      |
| Above 4G Decoding	                     | Enabled	  | 显存映射                       |
| CSM / Legacy Boot                      | 	Disabled | 	保持纯UEFI                   |
| ACS Enable（如果有这一项）                     | 	Enabled  | 	改善IOMMU分组，不要留Auto         |
| Re-Size BAR Support / Resizable BAR    | 	Disabled | 	RTX30系+VFIO开ReBAR偶有异常，先关掉 |

### 网络唤醒

| 项目	                                                       | 设置         | 	说明                               |
|-----------------------------------------------------------|------------|-----------------------------------|
| ErP / EuP Ready                                           | 	Disabled	 | 开启会切断 S5 下的 +5VSB，网卡完全没电，WOL 必然失效 |
| Deep Sleep / Deep S5                                      | Disabled   | 	同上                               |
| Wake on LAN / Power On by PCI-E / Resume by PCI-E Device	 | Enabled	   | 新主板走 PCIe 引脚唤醒                    |
| 板载网卡（Onboard LAN Controller）                              | 	Enabled   | 	别关                               |

## 镜像准备

+ 下载Debian13镜像：https://www.debian.org/distrib/
    + 推荐下载「完整安装映像」，在安装后期跳取消勾选桌面相关套件，安装完成后再手动安装gnome
    + 其次是Live Gnome，但是安装的是完整版gnome，没得选择安装范围，会有烦人的小游戏
    + 不推荐「小型安装映像」，需要强制联网下载，软件下载巨慢无比
+ 下载Windows11物联网版镜像：待补充

## 安装Debian

系统分区记得要设置格式化，其余没啥特别需要注意的，如果安装过程中U盘掉盘了，强制重启重装就好。
安装过程建议联网，不然安装完成后默认网卡是关闭的，在终端里操作联网并不方便。

| 分区    | 大小   | 类型               |
|-------|------|------------------|
| EFI分区 | 128M | EFI System       |
| 交换分区  | 16G  | Linux System     |
| 系统分区  | 剩余空间 | Linux filesystem |

不知道是不是在安装的时候少选了什么东西，连sudo都没安装，因此安装完成先切换到root账户进行操作。

```shell
# 在普通用户里切换root账号，su后输入root密码
su
```

### 国内源

终端里下载清华源配置文件进行替换，其余版本参考：https://mirrors.tuna.tsinghua.edu.cn/help/debian/

+ 长链
    + https://raw.githubusercontent.com/cellargalaxy/blog-code/refs/heads/master/source/_posts/%E6%8A%98%E8%85%BE/30.debian13-sources.list
+ 生成短链：https://shorturl.at/

```shell
cd /etc/apt
mv sources.list sources.list.back
wget https://tinyurl.com/XXXXX -O sources.list
```

### 更新系统

```shell
apt update
apt upgrade -y
```

### 用户提权

```shell
# 安装sudo
apt install -y sudo
# 把用户加入 sudo 组
usermod -aG sudo 用户名
# 验证是否加入 sudo 组
id 用户名
# 切换用户
su - 用户名
```

### 开放SSH

```shell
sudo apt install -y openssh-server
sudo systemctl enable ssh
sudo systemctl start ssh
# 远程连接试试
```

```shell
# 给sshd加OOM保护
sudo mkdir -p /etc/systemd/system/ssh.service.d
sudo tee /etc/systemd/system/ssh.service.d/oom.conf > /dev/null <<'EOF'
[Service]
OOMScoreAdjust=-1000
EOF
sudo systemctl daemon-reload
sudo systemctl restart ssh
# 验证
cat /proc/$(pgrep -o sshd)/oom_score_adj    # 期望 -1000
```

### 终端软件

```shell
sudo apt install -y git wget curl htop vim tmux at bridge-utils ethtool
sudo systemctl enable --now atd
```

### zsh

```shell
#我这使用zsh
sudo apt install -y zsh
sh -c "$(curl -fsSL https://install.ohmyz.sh/)"

#改zsh主题
vim ~/.zshrc
ZSH_THEME="gentoo"
source ~/.zshrc
```

### 配置网络桥接

```shell
# SSH断连防护
# 用 tmux 保护会话，断线后可重连
tmux new -s net
# 15 分钟后自动恢复原网络配置的保险
echo "cp /etc/network/interfaces.bak /etc/network/interfaces; ip link delete br0; systemctl restart networking" | at now + 15 minutes
atq    # 确认任务已建立，记下编号
```

```shell
# 用 ifupdown 配置桥接
ip -br addr                 # 记下网卡名，如 enp3s0 / eno1
ip route | grep default     # 记下网关

cp /etc/network/interfaces /etc/network/interfaces.bak

# 把 enp3s0 换成你的实际网卡名
cat > /etc/network/interfaces <<'EOF'
source /etc/network/interfaces.d/*
auto lo
iface lo inet loopback
# 物理网卡交给桥接，自己不配 IP
allow-hotplug enp3s0
iface enp3s0 inet manual
# 桥接设备，宿主机 IP 落在这里
auto br0
iface br0 inet dhcp
    bridge_ports enp3s0
    bridge_stp off
    bridge_fd 0
EOF
```

```shell
# 生效并验证
systemctl restart networking
ip -br addr show br0     # br0 应有 IP
ping -c 3 223.5.5.5      # 外网通
brctl show               # br0 的 interfaces 列有物理网卡
# 新开一个终端窗口测试 SSH 能否连上。确认无误后
atq          # 查看定时任务编号
atrm <编号>  # 删掉保险
```

### docker

```shell
curl -fsSL https://get.docker.com | bash -s docker

sudo systemctl enable docker
sudo systemctl start docker

# docker拉取镜像走代理，https://neucrack.com/p/286
sudo systemctl edit docker # 查询docker的service文件路径，添加在[Service]下
Environment="HTTP_PROXY=http://192.168.123.1:10808"
Environment="HTTPS_PROXY=http://192.168.123.1:10808"
Environment="NO_PROXY=localhost,127.0.0.1,192.168.123.1"
sudo systemctl daemon-reload                            # 重载service文件

sudo mkdir -p /etc/docker
sudo vim /etc/docker/daemon.json
{
  "log-driver":"json-file",
  "log-opts": {"max-size":"5m", "max-file":"3"}
}
sudo systemctl restart docker # 重启docker生效配置
sudo docker info              # 检查配置是否生效

sudo groupadd docker          # 创建docker用户组
sudo gpasswd -a $USER docker  # 将用户加入到docker用户组
newgrp docker                 # 更新用户组
docker run --rm hello-world   # 检查docker能否正常运行
```

### 禁止休眠

```shell
sudo systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target
systemctl status sleep.target | head -3    # 期望看到 masked
```

### 网络唤醒

```shell
ip -br link          # 查看网卡名，通常是 enp3s0 / eno1
sudo ethtool enp3s0 | grep -i wake
# Supports Wake-on: g 表示支持网络唤醒
# Wake-on: d 表示当前未启用

# 临时开启（重启失效，仅用于验证链路是否通）
sudo ethtool -s enp3s0 wol g

# 设置开机自动生效
cat > /etc/systemd/network/50-wol.link <<'EOF'
[Match]
OriginalName=enp3s0
[Link]
WakeOnLan=magic
EOF

udevadm trigger --action=add /sys/class/net/enp3s0
```

### 常用命令

```shell
# 查询文件与文件夹大小
du -h --max-depth=1
# 查询docker空间占用，镜像与容器
docker system df -v
# 清理未使用的资源（镜像、容器、卷、缓存等）
docker system prune -a
```

## 安装Windows

```shell
# 切 root，后续步骤都在 root 下操作，省得每条都加 sudo
sudo -i
```

### 安装KVM/libvirt

```shell
# 确认 CPU 支持虚拟化
lscpu | grep -E "Model name|Virtualization"
# 期望看到 `Virtualization: VT-x`

# 安装常用工具
apt install -y pciutils psmisc lsof dnsutils net-tools

# 安装KVM/libvirt
apt install -y qemu-system-x86 qemu-utils libvirt-daemon-system \
               libvirt-clients libvirt-daemon-config-network \
               virtinst ovmf swtpm swtpm-tools libvirt-dbus

# 启动服务并加权限
systemctl enable --now libvirtd      # 启动服务
usermod -aG libvirt,kvm $SUDO_USER   # 你的日常账号
systemctl is-active libvirtd         # 期望 active
virsh version
virsh list --all                     # 空列表，但不该报错
```

各包作用：

| 包                     | 作用                   | 漏装的后果              |
|-----------------------|----------------------|--------------------|
| qemu-system-x86       | 虚拟机运行核心              | 无法运行 VM            |
| libvirt-daemon-system | libvirtd 服务，虚拟机管理者   | Cockpit 虚拟机页面连不上   |
| virtinst              | 创建虚拟机的后端工具           | "Create VM" 按钮灰色   |
| ovmf                  | UEFI 固件              | Win11 装不了（必须 UEFI） |
| swtpm swtpm-tools     | 软件 TPM 2.0           | Win11 安装检测不通过      |
| libvirt-dbus          | Cockpit 与 libvirt 通信 | 界面部分功能异常           |

### 让libvirt能用桥接网络

```shell
cat > /tmp/br0.xml <<'EOF'
<network>
  <name>br0</name>
  <forward mode="bridge"/>
  <bridge name="br0"/>
</network>
EOF

virsh net-define /tmp/br0.xml
virsh net-start br0
virsh net-autostart br0
virsh net-list --all     # br0 应为 active / yes
```

### 安装Cockpit

```shell
. /etc/os-release
echo $VERSION_CODENAME     # 期望输出 trixie

# `-t ${VERSION_CODENAME}-backports` 表示优先从 backports 取包
apt install -y -t ${VERSION_CODENAME}-backports cockpit cockpit-machines
systemctl enable --now cockpit.socket
systemctl status cockpit.socket

# 建议加装的插件
# cockpit-podman是容器管理，装了可以在网页上看 Docker/Podman 容器状态（Docker 支持有限，主要面向 Podman，可按需选择）
apt install -y cockpit-storaged cockpit-networkmanager cockpit-podman
```

+ 浏览器打开，必须是https，用http会连不上
    + https://IP:9090
    + 用Linux的账号密码登录
+ 登录后，勾选左上角的「Administrative access」/「管理访问权限」（或登录时勾 "重用密码进行特权任务"）
    + 不勾的话，创建虚拟机等操作会因权限不足而失败
+ 左侧应出现「Virtual Machines」/「虚拟机」
+ 看不到就按顺序排查

```shell
# 1. 插件装上了吗
dpkg -l cockpit-machines
# 2. libvirtd 活着吗
systemctl status libvirtd
# 3. 重启 cockpit
systemctl restart cockpit.socket
# 然后 Ctrl + F5 强制刷新浏览器，Cockpit 前端有缓存
```

### 安装 NVIDIA 驱动

```shell
lspci -nn | egrep -i "3d|display|vga"                   # 查看GPU类型
apt install -y linux-headers-$(uname -r) linux-headers-amd64   # 构建NVIDIA驱动程序的内核头文件
apt install -y nvidia-driver firmware-misc-nonfree      # 安装NVIDIA驱动
```

```shell
# 让 nvidia 不接管显示输出，方便后续动态卸载
# 无桌面服务器不需要 `nvidia-drm modeset`。开着反而会导致 hook 里 `modprobe -r nvidia` 失败
cat > /etc/modprobe.d/nvidia-headless.conf <<'EOF'
options nvidia-drm modeset=0
EOF

update-initramfs -u
```

```shell
# 开启 IOMMU
# `intel_iommu=on` —— 开启 IOMMU，直通的硬性前提
# `iommu=pt` —— passthrough 模式，只对直通设备做 DMA 重映射，减少宿主机性能损耗
# *注意这里没有 `vfio-pci.ids=`，也没有黑名单 nvidia。这是刻意的：宿主机要能正常用 nvidia 驱动跑 Docker CUDA，GPU 归属由 hook 脚本动态切换。网上很多教程会写死绑定，那样 Docker 就永远用不了 CUDA 了

cp /etc/default/grub /etc/default/grub.bak

sed -i 's/^GRUB_CMDLINE_LINUX_DEFAULT=.*/GRUB_CMDLINE_LINUX_DEFAULT="quiet intel_iommu=on iommu=pt"/' \
    /etc/default/grub

grep GRUB_CMDLINE_LINUX_DEFAULT /etc/default/grub   # 确认改对了
update-grub
reboot
```

```shell
# 重启后验证（五项都要过）

# ① IOMMU 已启用
dmesg | grep -i -e DMAR -e IOMMU | head -20
# 期望看到 "DMAR: IOMMU enabled"

# ② 中断重映射
dmesg | grep -i -e "Interrupt remapping" -e "IRQ remapping"
# 期望看到 "DMAR-IR: Enabled IRQ remapping in x2apic mode" 之类
# 若看到 disabled 或完全没有输出，见下方处理

# ③ 驱动正常
nvidia-smi
# 期望看到 RTX 3060 和显存信息

# ④ modeset 已关闭
cat /sys/module/nvidia_drm/parameters/modeset
# 期望 N。若为 Y，检查 /lib/modprobe.d/ 下是否有nvidia包自带的冲突配置

# ⑤ 记录 GPU 的 PCI 地址（后面反复要用，抄下来）
lspci -nn | grep -i nvidia
# 第 ⑤ 步典型输出
# 01:00.0 VGA compatible controller [0300]: NVIDIA Corporation GA106 [GeForce RTX 3060] [10de:2504] (rev a1)
# 01:00.1 Audio device [0403]: NVIDIA Corporation GA106 High Definition Audio Controller [10de:228e] (rev a1)
# 01:00.0 和 01:00.1 必须一起直通（显卡本体 + HDMI 音频），只直通一个会导致 Windows 里驱动异常

# 检查 IOMMU 分组
for g in $(find /sys/kernel/iommu_groups/* -maxdepth 0 -type d | sort -V); do
    echo "IOMMU 组 ${g##*/}:"
    for d in $g/devices/*; do
        echo -e "\t$(lspci -nns ${d##*/})"
    done
done
# 在输出里找到含 `01:00.0` 的那一组
```

| 组内情况                   | 	结论                     |
|------------------------|-------------------------|
| 只有 01:00.0 + 01:00.1	  | ✅ 完美，可直接直通              |
| 还有同一插槽的 PCIe 桥	        | ✅ 一般也没问题                |
| 混着 SATA / USB / 网卡控制器	 | ❌ 麻烦。整组必须一起直通，会导致宿主机丢硬盘 |

### 安装Docker支持CUDA

```shell
curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | sudo gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg \
  && curl -s -L https://mirrors.ustc.edu.cn/libnvidia-container/stable/deb/nvidia-container-toolkit.list | \
    sed 's#deb https://nvidia.github.io#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://mirrors.ustc.edu.cn#g' | \
    sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
sudo apt update
sudo apt install -y nvidia-container-toolkit

sudo nvidia-ctk runtime configure --runtime=docker                                  # 配置docker运行时
sudo systemctl restart docker                                                       # 重启docker
cat /etc/docker/daemon.json                                                         # 检测docker配置
docker run --rm --gpus all nvidia/cuda:12.2.2-cudnn8-runtime-ubuntu22.04 nvidia-smi # 验证
docker run --rm --gpus all nvidia/cuda:12.2.2-devel-ubuntu22.04 nvcc -V             # 验证

# 关掉 nvidia-persistenced，它会常驻占用 GPU，导致后面 `modprobe -r nvidia` 失败
systemctl disable --now nvidia-persistenced 2>/dev/null || true
```

### 创建虚拟机（先不直通 GPU）

```shell
mkdir -p /var/lib/libvirt/images/iso
cd /var/lib/libvirt/images/iso

# 将Windows镜像上传到/var/lib/libvirt/images/iso中

# 下载 virtio 驱动（Windows 装机必需，否则看不到硬盘）
wget https://fedorapeople.org/groups/virt/virtio-win/direct-downloads/stable-virtio/virtio-win.iso

# 镜像与驱动的iso建议保留不删除，配置会引用到，删除可能会导致报错，也便于后续维护和救援
```

+ Cockpit 界面操作
+ 进 虚拟机 → Create VM

| 项	                      | 值	                    | 说明                |
|-------------------------|-----------------------|-------------------|
| Name	                   | win11	                | hook 脚本里要用，记住这个名字 |
| Installation type	      | Local install media	  | 选你的 ISO           |
| Operating system	       | Microsoft Windows 11	 | 影响默认硬件优化          |
| Storage                 | 	创建 80 GB 卷           | 	剪辑素材另挂盘          |
| Memory                  | 	12 GiB	              | 见下方说明             |
| ☑ Immediately start VM	 | 不勾	                   | 需要先改配置            |

+ Cockpit 界面无法配置 TPM 和某些细节，需要手工改 XML

```shell
export EDITOR=vim
# 改之前先看清Cockpit生成了什么，避免下面的修改和它冲突 todo，这里要看的是什么
virsh dumpxml win11 | grep -i -E "boot|machine|loader|nvram"
virsh dumpxml win11 | grep -A3 cdrom
virsh edit win11
```

```
+ UEFI 固件（Win11 强制要求）
+ 找到 `<os>` 段
    + `firmware="efi"` 是让libvirt自动挑固件
    + `machine` 必须是 `q35`（不能是 `pc-i440fx`），否则 PCIe 直通无法工作
    + 如果已有 `boot` 配置则无需添加 `boot`，否则添加才在 <os> 段里加下面两行

  <os firmware="efi">
    <type arch="x86_64" machine="q35">hvm</type>
    <boot dev="cdrom"/>
    <boot dev="hd"/>
  </os>
```

```
+ TPM 2.0（Win11 安装检测）
+ 在 `</devices>` 前加
    + 24H2 及更新的 IoT Enterprise 已把 TPM 列为可选，但 21H2~23H2 仍要求，加上没成本

    <tpm model="tpm-crb">
      <backend type="emulator" version="2.0"/>
    </tpm>
```

```
+ 模拟声卡
+ 在 `</devices>` 前加
    + 作用是让 Windows 里始终存在一个音频输出设备
    + RDP 断开后台跑导出任务时，剪辑软件不会因 "未安装音频设备" 报错
    + 真正的声音传输走 RDP 重定向

    <sound model="ich9">
      <audio id="1"/>
    </sound>
    <audio id="1" type="none"/>
```

```
+ 关闭内存气球
+ 找到 `<memballoon>` 改为

    <memballoon model="none"/>
```

```
+ 内存锁定上限
+ 在 `<currentMemory>` 之后加
    + 作用是让超限时「虚拟机拒绝启动并报错」，而不是拖着宿主机一起OOM
    + 失败要失败在可控的地方
    + 数值 = guest内存 + 1GiB，换算成KiB。下例对应 12GiB guest：(12+1)*1024*1024 = 13631488

  <memtune>
    <hard_limit unit="KiB">13631488</hard_limit>
  </memtune>
```

```
+ CPU 拓扑
+ 例如4核8线程，给6个vCPU，留2个给宿主机
    + `cores=3 threads=2` 让 Windows 正确识别超线程拓扑，调度效率优于报成 6 个独立核心

  <vcpu placement="static">6</vcpu>
  <cpu mode="host-passthrough" check="none" migratable="off">
    <topology sockets="1" dies="1" cores="3" threads="2"/>
  </cpu>
```

```
+ 挂载 virtio 驱动光驱
+ 在 `<devices>` 里加第二个光驱
    + target dev 不能和安装ISO冲突。先用前面的 grep -A3 cdrom 看安装ISO占了哪个 todo，这里要看的是什么
      它一般是 sda，那这里用 sdb；如果它已经是 sdb，这里就改成 sdc

    <disk type="file" device="cdrom">
      <driver name="qemu" type="raw"/>
      <source file="/var/lib/libvirt/images/iso/virtio-win.iso"/>
      <target dev="sdb" bus="sata"/>
      <readonly/>
    </disk>
```

```
+ 不要删除模拟显卡 todo，是指在virsh edit win11删除吗
+ Cockpit 创建虚拟机时会自带一个 <video> 设备（vga 或 qxl），务必保留它
+ 这是单显卡直通场景下最重要的救援通道：
    + Windows 里会同时存在「模拟显卡 + RTX 3060」两个适配器
    + 桌面渲染走模拟显卡，所以 Cockpit 的 VNC 控制台一直可用，不会失联
    + 3060 作为纯计算卡存在，CUDA / NVENC 正常可用，RDP 看桌面无压力
+ 很多教程会让你直通后删掉video，那样一旦RDP不通就彻底没有入口了
```

```shell
# 改完存盘后校验配置能否通过
virsh dumpxml win11 > /dev/null && echo "XML OK"
```

+ 回到 Cockpit → 点 Run → 展开 Console（VNC）
    + "找不到驱动器" —— 这是正常的，virtio 硬盘需要手动加载驱动
    + 点「加载驱动程序」→「浏览」
    + 选第二个光驱（virtio-win）→ `viostor` → `w11` → `amd64`
    + 确定后硬盘出现
    + 同样方式再加载 `NetKVM\w11\amd64`（网卡驱动）
+ 进入桌面后，在 Windows 里
    + 打开 virtio-win 光驱，运行 virtio-win-guest-tools.exe（安装全部 virtio 驱动 + qemu-ga 客户机代理）
    + 设置 → 系统 → 远程桌面 → 开启
    + 把网络位置改为「专用网络」。判为「公用网络」时防火墙会拦 3389，这是RDP连不上的常见原因
    + 关闭"快速启动"！控制面板 → 电源选项 → 选择电源按钮的功能 → 更改当前不可用的设置 → 取消勾选"启用快速启动"
    + 再以管理员身份打开 CMD 执行 `powercfg /h off` 彻底关闭休眠
        + 休眠同样会导致 Windows 不真正释放 GPU，是「来回切换偶发失败」的常见根因
+ 关机后确认
    + Windows 能正常启停
    + 在局域网内有独立 IP，能从你的电脑 RDP 连上
    + RDP 里声音正常（客户端 → 显示选项 → 本地资源 → 远程音频 → "在此计算机上播放"）
    + `virsh dumpxml win11 | grep -i tpm` 有输出
+ 全部通过再进行下一步。这是整个流程的最后一个安全检查点 —— 此后开始动 GPU，出问题排查难度陡增

### 配置 GPU 直通

```shell
#!/bin/bash
#==============================================================
# libvirt hook - GPU 直通自动切换
#   prepare/begin : 自动停止占用 GPU 的容器 → 卸载 nvidia → 交给 vfio
#   release/end   : 收回 GPU → 恢复 nvidia 驱动（容器需手动重启）
#==============================================================

# libvirt hook 的环境变量极简，PATH 必须显式设置，否则找不到 docker
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

VM_NAME="win11"                                   # ← 改成你的虚拟机名
GPU_ADDR="0000:01:00.0"                           # ← 改成你实际的 GPU PCI 地址
LOG="/var/log/libvirt-gpu-hook.log"
STATE="/var/lib/libvirt/gpu-stopped-containers"    # 记录被停掉的容器
STOP_TIMEOUT=8                                    # 单个容器停止等待秒数

[ "$1" != "$VM_NAME" ] && exit 0

log() { echo "[$(date '+%F %T')] $*" >> "$LOG"; }

# 日志超过 1MB 自动截断，防止无限增长
[ -f "$LOG" ] && [ "$(stat -c%s "$LOG")" -gt 1048576 ] && tail -500 "$LOG" > "$LOG.tmp" && mv "$LOG.tmp" "$LOG"

#--------------------------------------------------------------
# 判断容器是否使用 GPU（覆盖三种常见写法）
#--------------------------------------------------------------
uses_gpu() {
    local c="$1" req rt env
    # ① --gpus all（现代写法）
    req=$(docker inspect -f '{{.HostConfig.DeviceRequests}}' "$c" 2>/dev/null)
    [ -n "$req" ] && [ "$req" != "[]" ] && return 0
    # ② --runtime=nvidia（旧写法）
    rt=$(docker inspect -f '{{.HostConfig.Runtime}}' "$c" 2>/dev/null)
    [ "$rt" = "nvidia" ] && return 0
    # ③ NVIDIA_VISIBLE_DEVICES 环境变量（更旧的 nvidia-docker）
    env=$(docker inspect -f '{{range .Config.Env}}{{println .}}{{end}}' "$c" 2>/dev/null)
    echo "$env" | grep -q '^NVIDIA_VISIBLE_DEVICES=' && return 0
    return 1
}

#--------------------------------------------------------------
# 当前设备绑在哪个驱动上
#--------------------------------------------------------------
cur_driver() {
    basename "$(readlink -f /sys/bus/pci/devices/$GPU_ADDR/driver 2>/dev/null)" 2>/dev/null
}

#--------------------------------------------------------------
case "$2/$3" in
  "prepare/begin")
      log "===== 启动 $VM_NAME：准备移交 GPU ====="

      # ---- 1. 自动停止占用 GPU 的容器 ----
      : > "$STATE"
      if command -v docker >/dev/null 2>&1 && docker info >/dev/null 2>&1; then
          found=0
          for c in $(docker ps -q 2>/dev/null); do
              if uses_gpu "$c"; then
                  name=$(docker inspect -f '{{.Name}}' "$c" 2>/dev/null | sed 's|^/||')
                  echo "$name" >> "$STATE"
                  log "  停止 GPU 容器: $name"
                  docker stop -t "$STOP_TIMEOUT" "$c" >/dev/null 2>&1
                  found=1
              fi
          done
          [ "$found" -eq 0 ] && log "  未发现运行中的 GPU 容器"
      else
          log "  Docker 未安装或未运行，跳过容器检查"
      fi

      # ---- 2. 停止常驻占用 GPU 的服务 ----
      systemctl stop nvidia-persistenced 2>/dev/null

      # ==============================================
      # ↓↓↓ 单显卡专用：有亮机卡可以删掉这整段 ↓↓↓
      # ==============================================
      for vt in /sys/class/vtconsole/vtcon*/bind; do
          [ -w "$vt" ] && echo 0 > "$vt" 2>/dev/null
      done
      for drv in simple-framebuffer efi-framebuffer; do
          [ -d "/sys/bus/platform/drivers/$drv" ] || continue
          for dev in /sys/bus/platform/drivers/$drv/*; do
              [ -e "$dev/driver" ] || continue
              basename "$dev" > "/sys/bus/platform/drivers/$drv/unbind" 2>/dev/null
          done
      done
      sleep 2
      # ==============================================
      # ↑↑↑ 单显卡专用段落结束 ↑↑↑
      # ==============================================

      # ---- 3. 卸载 nvidia 模块 ----
      # 逐个卸载并容错。原因：如果其中某个模块本来就没加载，
      # 一条 modprobe -r 带多个模块会整体返回失败，
      # 脚本会误判成"卸载失败"并 exit 1，从而阻止虚拟机启动
      for m in nvidia_drm nvidia_modeset nvidia_uvm nvidia; do
          if lsmod | grep -q "^$m "; then
              modprobe -r "$m" 2>>"$LOG"
          fi
      done

      # 用"还有没有nvidia模块残留"作为真正的判断依据
      if lsmod | grep -q '^nvidia'; then
          log "!! nvidia 模块卸载失败，诊断信息如下："
          log "---- 仍加载的模块 ----"
          lsmod | grep -E '^nvidia|^vfio' >>"$LOG" 2>&1
          log "---- 占用 /dev/nvidia* 的进程 ----"
          fuser -v /dev/nvidia* >>"$LOG" 2>&1
          log "---- 当前运行的容器 ----"
          docker ps >>"$LOG" 2>&1
          log "---- nvidia-smi ----"
          nvidia-smi >>"$LOG" 2>&1
          log "!! 已中止启动，请处理上述占用后重试"
          exit 1
      fi
      log "  nvidia 模块已全部卸载"

      # ---- 4. 加载 vfio ----
      modprobe vfio-pci
      log "===== 就绪，libvirt 接管 GPU ====="
      ;;

  "release/end")
      log "===== $VM_NAME 已停止：收回 GPU ====="

      # 等待 libvirt 把设备从 vfio-pci 解绑（最多等 20 秒）
      # 这里必须等：hook 的 release/end 可能跑在 libvirt 归还设备【之前】，
      # 那时急着 modprobe nvidia，模块会加载成功但抓不到卡，
      # nvidia-smi 报"未找到设备" —— 这就是"偶发失败"的主要来源
      for i in $(seq 1 20); do
          cur=$(cur_driver)
          [ "$cur" != "vfio-pci" ] && break
          sleep 1
      done
      log "  设备当前驱动: ${cur:-none}"

      modprobe -r vfio_pci vfio_iommu_type1 vfio 2>/dev/null
      sleep 1

      if modprobe nvidia && modprobe nvidia_uvm; then
          log "  nvidia 驱动已恢复"
      else
          log "!! nvidia 驱动恢复失败，请检查：dmesg | tail -30"
      fi

      # ---- 提示需要手动恢复的容器（不自动启动）----
      if [ -s "$STATE" ]; then
          log "  以下容器此前被自动停止，需手动启动："
          while read -r name; do
              [ -n "$name" ] && log "      docker start $name"
          done < "$STATE"
      fi
      log "===== 完成 ====="
      ;;
esac
exit 0
```

```shell
# 把上面的脚本内容落盘
mkdir -p /etc/libvirt/hooks
vim /etc/libvirt/hooks/qemu
# 粘贴上面的内容，注意改 VM_NAME 和 GPU_ADDR 两个变量

chmod +x /etc/libvirt/hooks/qemu
bash -n /etc/libvirt/hooks/qemu && echo "语法 OK"    # 先做语法检查
systemctl restart libvirtd
```

+ 添加 PCI 设备
+ Cockpit 界面：虚拟机页面 → 下方 Host devices → Add host device → Type 选 PCI → 勾选 显卡 的两个条目（VGA + Audio）→ Add

```shell
# 确认是 managed 模式，这决定了libvirt会不会自动绑定/解绑vfio-pci
virsh dumpxml win11 | grep hostdev
# 期望看到 managed='yes'
# 如果不是 yes，libvirt 不会自动切换驱动，你得在 hook 里手动 bind/unbind
```

```shell
# 首次启动测试

# 开一个窗口盯日志
tail -f /var/log/libvirt-gpu-hook.log
# 另一个窗口启动
virsh start win11

# 预期日志
# [...] ===== 启动 win11：准备移交 GPU =====
# [...]   停止 GPU 容器: comfyui
# [...]   nvidia 模块已全部卸载
# [...] ===== 就绪，libvirt 接管 GPU =====

# 宿主机侧验证
lspci -nnk -s 01:00.0   # Kernel driver in use: vfio-pci   ← 成功标志
nvidia-smi              # 报错"未找到设备"是正确的，说明显卡确实交出去了

# 盯一眼内存余量，这是16G机器的关键指标
grep -E 'MemAvailable|Mlocked' /proc/meminfo
```

+ RDP 进 Windows
    + 设备管理器确认 RTX 3060 已识别，且没有黄色感叹号（代码43）
+ 切换回 Linux 测试
    + 来回切换至少三次，确认稳定
    + 偶发失败通常是因为 Windows 侧没关"快速启动"或没执行 `powercfg /h off`，或有容器没停干净

```shell
virsh shutdown win11 # 或者 Windows 里点关机（不是睡眠）
sleep 30

nvidia-smi              # 应重新看到 3060
lspci -nnk -s 01:00.0   # Kernel driver in use 应回到 nvidia
docker run --rm --gpus all nvidia/cuda:12.2.2-cudnn8-runtime-ubuntu22.04 nvidia-smi
```

### 日常使用流程

+ 切到 Windows
    + 浏览器打开 https://IP:9090 → 虚拟机 → win11 → Run → 等 1 分钟 → RDP 连接
    + 使用 CUDA 的 Docker 容器会被 hook 自动停止，无需手动操作
+ 切回 Linux
    + Windows 里点关机（不是睡眠）→ 30 秒后宿主机自动恢复 CUDA
+ 出问题先看日志
    + `tail -50 /var/log/libvirt-gpu-hook.log`
    + `dmesg -T | tail -30`
